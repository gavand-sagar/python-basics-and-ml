# Lab 5: Classes and objects
# C#: class Person { public string Name; public Person(string name) { Name = name; } public string Greet() { return "Hi, " + Name; } }
# Python: class Person:, __init__(self, name), self.name, def greet(self): return ...

# TODO: Define class Person with:
#   __init__(self, name) storing name in self.name
#   method greet(self) that returns "Hi, " + self.name
# Create p = Person("Alice") and print p.greet()  (no "new" keyword)
