# Lab 4: Arrays in Python – lists, slicing, tuples, dicts, sets, strings

lst = [10, 20, 30, 40, 50, 60]

# TODO: Return a new list with every second element (10, 30, 50). Use slicing lst[::2].
# Then implement the same with a for loop and compare.


# TODO: Set s = "hello". Print s[1:4] and s[::-1].


# TODO: Create a tuple t = (1, 2, 3), a dict d = {"a": 1, "b": 2}, and a set (e.g. {1, 2, 2, 3}).
# Print t[0], d["a"], and the set (notice no duplicates).
